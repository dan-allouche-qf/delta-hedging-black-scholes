# Runtime + dev dependencies. The package metadata in pyproject.toml is the
# source of truth; these floors are the versions the project is tested against.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0

# Notebook + tooling
ipykernel>=6.0
nbconvert>=7.0
nbformat>=5.0

# Dev: tests and linting
pytest>=7.4
ruff>=0.4
